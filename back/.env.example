# ============================================
# DATABASE (Docker Compose)
# ============================================
# Credenciais configuradas no docker-compose.yml
# User: ih, Password: ih, Database: insighthouse
DATABASE_URL="postgresql://ih:ih@localhost:5432/insighthouse?schema=public"
DIRECT_URL="postgresql://ih:ih@localhost:5432/insighthouse?schema=public"

# ============================================
# APPLICATION
# ============================================
PORT=3001
NODE_ENV=development

# ============================================
# FRONTEND
# ============================================
FRONTEND_URL="http://localhost:3000"

# ============================================
# SECURITY
# ============================================
# GERE UM SECRET SEGURO:
# Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# Linux/Mac: openssl rand -base64 32
# Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# ============================================
# API
# ============================================
API_BASE_URL="http://localhost:3001"

# ============================================
# DOCKER COMPOSE (referência)
# ============================================
# O docker-compose.yml na raiz do projeto já está configurado:
# - PostgreSQL 16 na porta 5432
# - pgAdmin na porta 5050 (http://localhost:5050)
#   Email: admin@insighthouse.local
#   Password: admin
#
# Para iniciar: docker-compose up -d
# Para parar: docker-compose stop
# Para remover: docker-compose down
# Para remover com dados: docker-compose down -v
